# for ease of tracking, this file is for home-manager options that are defined in `modules/home-manager/myHomeConfig`
# if a default should apply to all users,
# consider making it the default in the `myHomeConfig` module, instead of declaring it here
_:
{
  myHomeConfig.npm.enable = true;
}
